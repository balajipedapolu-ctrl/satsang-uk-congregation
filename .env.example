# Copy this file to ".env.local" for local development,
# and set the same variable in Vercel (Settings → Environment Variables).

# URL of your Google Apps Script Web App that appends registrations to a Sheet.
# Setup guide: GOOGLE_SHEETS_SETUP.md
GOOGLE_SHEETS_WEBHOOK_URL=
